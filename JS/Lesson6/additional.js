//      1) Напишіть функцію cutString(str, n), которая ділить строку на під строки, складаючі из n символів.
//      document.writeln(cutString('насолодження',3)) // [нас,лаж,ден,ие]

//      let cutString = (str, n) => {
//      let share = [];
//      for (let i = 0; i < str.length; i=i+3) {
//      share.push(str.substr(i,n));
//      }
//      return share;
//      }
//
//      document.write(cutString('насолодження',3));
//      console.log(cutString('насолодження',3));


